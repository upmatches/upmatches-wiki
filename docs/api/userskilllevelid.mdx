---
id: user-skill-level-id
title: UserSkillLevelId Class
sidebar_label: UserSkillLevelId
---

## Purpose of the UserSkillLevelId Class

The `UserSkillLevelId` class is an embeddable entity used to represent a composite key in a database. This class is part of the `com.upmatches.app.features.user.entities` package and is designed to be used with JPA (Java Persistence API) to uniquely identify a user's skill level by combining a `UUID` for the user and a `Long` for the skill level. The class implements `Serializable` to ensure that instances can be serialized, which is a requirement for entities used in JPA.

## Public Methods

The `UserSkillLevelId` class provides the following public methods:

| Method Signature               | Description                                                                 |
|--------------------------------|-----------------------------------------------------------------------------|
| `UserSkillLevelId()`           | Default constructor for creating an instance of `UserSkillLevelId`.         |
| `UserSkillLevelId(UUID userId, Long skillLevelId)` | Constructor that initializes the `userId` and `skillLevelId` fields. |
| `UUID getUserId()`             | Returns the `UUID` associated with the user.                                |
| `Long getSkillLevelId()`       | Returns the `Long` representing the skill level ID.                         |
| `boolean equals(Object o)`     | Compares this object to the specified object for equality.                  |
| `int hashCode()`               | Returns a hash code value for the object.                                   |

## Method Details

### `UserSkillLevelId()`

- **Description**: Default constructor for creating an instance of `UserSkillLevelId`.
- **Parameters**: None
- **Return Type**: None

### `UserSkillLevelId(UUID userId, Long skillLevelId)`

- **Description**: Constructor that initializes the `userId` and `skillLevelId` fields.
- **Parameters**:
  - `UUID userId`: The unique identifier for the user.
  - `Long skillLevelId`: The identifier for the skill level.
- **Return Type**: None

### `UUID getUserId()`

- **Description**: Returns the `UUID` associated with the user.
- **Parameters**: None
- **Return Type**: `UUID`

### `Long getSkillLevelId()`

- **Description**: Returns the `Long` representing the skill level ID.
- **Parameters**: None
- **Return Type**: `Long`

### `boolean equals(Object o)`

- **Description**: Compares this object to the specified object for equality. It checks if both `userId` and `skillLevelId` are equal.
- **Parameters**:
  - `Object o`: The object to compare with.
- **Return Type**: `boolean`

### `int hashCode()`

- **Description**: Returns a hash code value for the object, which is computed based on `userId` and `skillLevelId`.
- **Parameters**: None
- **Return Type**: `int`