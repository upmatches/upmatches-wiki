---
id: create-game-bookmark-request
title: CreateGameBookmarkRequest
sidebar_label: CreateGameBookmarkRequest
---

## Overview

The `CreateGameBookmarkRequest` class is a Java record used in the `com.upmatches.app.features.game.requests` package. It is designed to encapsulate the data required to create a bookmark for a game. This record ensures that the `gameId` is provided and is not null, leveraging the `@NotNull` annotation for validation.

## Public Methods

Since `CreateGameBookmarkRequest` is a Java record, it automatically provides the following methods:

### Constructor

| Method Signature | Description |
| ---------------- | ----------- |
| `CreateGameBookmarkRequest(@NotNull UUID gameId)` | Constructs a new `CreateGameBookmarkRequest` instance with the specified `gameId`. |

### Accessor Methods

| Method | Description | Return Type |
| ------ | ----------- | ----------- |
| `gameId()` | Returns the `UUID` of the game to be bookmarked. | `UUID` |

## Parameters

The `CreateGameBookmarkRequest` record has the following parameter:

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `gameId`  | `UUID` | The unique identifier of the game to be bookmarked. Must not be null. |

## Usage

This record is typically used in scenarios where a client needs to send a request to bookmark a game, ensuring that the request contains a valid `gameId`. The use of the `@NotNull` annotation helps in validating the input data before processing the request.