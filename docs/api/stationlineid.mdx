---
id: station-line-id
title: StationLineId Class
sidebar_label: StationLineId
---

import { useBaseUrl } from '@docusaurus/core';

## Overview

The `StationLineId` class is a part of the `com.upmatches.app.features.venue.entities` package. It is an embeddable entity used to represent a composite key for a station line in a transit system. This class implements the `Serializable` interface, ensuring that instances of `StationLineId` can be serialized, which is essential for their use in distributed systems or when storing them in a database.

## Class Details

The `StationLineId` class is marked with the `@Embeddable` annotation, indicating that it is intended to be embedded within another entity. It uses the Lombok `@Getter` annotation to automatically generate getter methods for its fields.

### Fields

- **stationId**: A `Long` representing the unique identifier of a station.
- **transitLineCode**: A `String` representing the code of the transit line, with a maximum length of 10 characters.

### Constructors

- **StationLineId()**: A protected no-argument constructor required by JPA.
- **StationLineId(Long stationId, String transitLineCode)**: A public constructor to create an instance of `StationLineId` with specified `stationId` and `transitLineCode`.

### Methods

Below is a table documenting the public methods of the `StationLineId` class:

| Method Signature | Description |
| ---------------- | ----------- |
| `boolean equals(Object o)` | Compares this `StationLineId` to the specified object. Returns `true` if the objects are equal, otherwise `false`. |
| `int hashCode()` | Returns a hash code value for the object, which is used in hashing based collections like `HashMap`. |

### Usage

The `StationLineId` class is typically used as a composite key in JPA entities. It is crucial for scenarios where a combination of `stationId` and `transitLineCode` uniquely identifies a record in the database.