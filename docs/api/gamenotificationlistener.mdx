---
id: game-notification-listener
title: GameNotificationListener
sidebar_label: GameNotificationListener
---

# GameNotificationListener

The `GameNotificationListener` is a Spring component that listens for game-related events and creates and delivers notifications to the appropriate recipients. It handles various game events such as players joining/leaving, game updates, cancellations, and skill-level warnings.

## Purpose

This listener is responsible for:
- Listening to `GameNotificationEvent` events after database transactions commit
- Resolving the appropriate recipients for each notification type
- Creating in-app notifications
- Delivering notifications via push and email channels
- Preventing duplicate notifications for certain event types

## Dependencies

| Dependency | Type | Description |
|------------|------|-------------|
| `GameRepository` | Repository | Retrieves game entities |
| `GameParticipantRepository` | Repository | Queries game participants |
| `NotificationRepository` | Repository | Checks for existing notifications |
| `ICreateNotificationService` | Service | Creates notification records |
| `IPushNotificationService` | Service | Sends push notifications |
| `IEmailNotificationService` | Service | Sends email notifications |
| `ShareLinkProperties` | Configuration | Provides web base URL for game links |

## Public Methods

### `onGameNotification(GameNotificationEvent event)`

The main event handler method, annotated with `@Async` and `@TransactionalEventListener`.

| Parameter | Type | Description |
|-----------|------|-------------|
| `event` | `GameNotificationEvent` | The game notification event containing type, game ID, and actor ID |

**Behavior:**
1. Retrieves the game from the repository
2. Resolves recipients based on event type
3. Builds notification title and message
4. Creates notification for each recipient (with deduplication check)
5. Attempts external delivery (push, then email)

## Private Methods

### `deliverExternally(Notification notification, EmailContext emailContext)`

Attempts to deliver notification via push, falling back to email.

| Parameter | Type | Description |
|-----------|------|-------------|
| `notification` | `Notification` | The notification to deliver |
| `emailContext` | `EmailContext` | Context data for email delivery |

**Delivery Flow:**
1. Attempts push notification via `pushNotificationService`
2. If push fails (not `SENT`), checks if recipient has an email
3. If email exists, sends via `emailNotificationService`
4. Logs if only in-app delivery is possible

### `isDedupable(NotificationType type)`

Determines if a notification type should be checked for duplicates.

| Parameter | Type | Description |
|-----------|------|-------------|
| `type` | `NotificationType` | The type of notification |

**Returns:** `boolean` - `true` for `PLAYER_JOINED`, `GAME_FULL`, `PLAYER_WARNING_MISMATCH`; `false` otherwise

### `resolveRecipients(GameNotificationEvent event, Game game)`

Determines the set of recipient IDs based on event type.

| Parameter | Type | Description |
|-----------|------|-------------|
| `event` | `GameNotificationEvent` | The event containing type and actor ID |
| `game` | `Game` | The game entity |

**Returns:** `Set<UUID>` - Set of recipient user IDs

**Recipient Resolution by Event Type:**

| Event Type | Recipients |
|------------|------------|
| `PLAYER_JOINED`, `PLAYER_LEFT`, `GAME_FULL`, `PLAYER_WARNING_MISMATCH` | Game organizer (excluding the actor) |
| `GAME_UPDATED`, `GAME_REMOVED` | All active participants (excluding the actor) |
| `PLAYER_REMOVED_FROM_GAME` | The actor (removed player) |

### `buildTitle(NotificationType type)`

Generates the notification title based on event type.

| Parameter | Type | Description |
|-----------|------|-------------|
| `type` | `NotificationType` | The type of notification |

**Returns:** `String` - The notification title

### `buildMessage(NotificationType type, String gameTitle)`

Generates the notification message based on event type and game title.

| Parameter | Type | Description |
|-----------|------|-------------|
| `type` | `NotificationType` | The type of notification |
| `gameTitle` | `String` | The title of the game (may be null or blank) |

**Returns:** `String` - The notification message

## Notification Types and Content

| Notification Type | Title | Message |
|-------------------|-------|---------|
| `PLAYER_JOINED` | A player joined your game | A new player joined [game title]. |
| `PLAYER_LEFT` | A player left your game | A player left [game title]. |
| `PLAYER_WARNING_MISMATCH` | Skill-level warning | A player joined your game. |
| `GAME_FULL` | Your game is full | Your badminton game has max out. |
| `GAME_UPDATED` | Game updated | [game title] has been updated. |
| `GAME_REMOVED` | Game cancelled | Your badminton game has been cancelled. |
| `PLAYER_REMOVED_FROM_GAME` | You were removed from a game | You have been removed from a badminton game. |
| Default | Notification | Update from Upmatches. |

## Configuration

The listener uses `ShareLinkProperties` to construct game URLs:
```
{webBaseUrl}/games/{gameId}
```

## Error Handling

- Logs warning if game is not found
- Catches and logs exceptions during notification creation/delivery
- Continues processing remaining recipients if one fails