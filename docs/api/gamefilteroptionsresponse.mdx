---
id: game-filter-options-response
title: GameFilterOptionsResponse
sidebar_label: GameFilterOptionsResponse
---

import com.upmatches.app.features.game.enums.GameType;
import com.upmatches.app.features.skilllevels.responses.SkillLevelResponse;
import com.upmatches.app.features.venue.responses.VenueResponse;

## Overview

The `GameFilterOptionsResponse` class is a record in the `com.upmatches.app.features.game.responses` package. It is designed to encapsulate various filtering options for games within the application. This record provides a structured way to return multiple filter criteria that can be used to refine game searches or selections.

## Public Methods

Since `GameFilterOptionsResponse` is a record, it automatically provides public methods to access its components. Below is a table documenting these methods, their parameters, and return types.

### Methods

| Method Name          | Parameters | Return Type                  | Description                                                                 |
|----------------------|------------|------------------------------|-----------------------------------------------------------------------------|
| `venues`             | None       | `List<VenueResponse>`        | Returns a list of venue responses available for filtering games.            |
| `skillLevels`        | None       | `List<SkillLevelResponse>`   | Returns a list of skill level responses available for filtering games.      |
| `availableStartTimes`| None       | `List<String>`               | Returns a list of available start times for filtering games.                |
| `gameTypes`          | None       | `List<GameType>`             | Returns a list of game types available for filtering games.                 |

## Usage

The `GameFilterOptionsResponse` record is typically used to encapsulate and return a set of filter options that can be applied when searching for games. Each component of the record provides a specific type of filter, such as venues, skill levels, start times, or game types, allowing for flexible and comprehensive game filtering capabilities.