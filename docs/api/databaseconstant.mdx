---
id: database-constant
title: DatabaseConstant
sidebar_label: DatabaseConstant
---

# DatabaseConstant

The `DatabaseConstant` class is a utility class that centralizes all database-related constants used across the Upmatches application. It provides a single source of truth for JDBC URL prefixes, HikariCP connection pool defaults, PostgreSQL and H2 database-specific configuration parameters, and logging message templates.

This class is declared as `final` and has a private constructor to prevent instantiation, following the standard utility class pattern.

## Constants

### JDBC URL Prefixes

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `JDBC_H2` | `String` | `"jdbc:h2:"` | JDBC URL prefix for H2 database |
| `JDBC_POSTGRESQL` | `String` | `"jdbc:postgresql:"` | JDBC URL prefix for PostgreSQL |
| `JDBC_MYSQL` | `String` | `"jdbc:mysql:"` | JDBC URL prefix for MySQL |

### HikariCP Connection Pool Defaults

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `DEFAULT_POOL_NAME` | `String` | `"hikari-pool"` | Default HikariCP connection pool name |
| `DEFAULT_APPLICATION_NAME` | `String` | `"Upmatches"` | Default application name for database connections |
| `DEFAULT_MAXIMUM_POOL_SIZE` | `int` | `10` | Maximum number of connections in the pool |
| `DEFAULT_MINIMUM_IDLE` | `int` | `10` | Minimum number of idle connections in the pool |
| `DEFAULT_CONNECTION_TIMEOUT` | `long` | `30000` (30 seconds) | Maximum time to wait for a connection from the pool (in milliseconds) |
| `DEFAULT_IDLE_TIMEOUT` | `long` | `600000` (10 minutes) | Maximum time a connection can remain idle in the pool (in milliseconds) |
| `DEFAULT_MAX_LIFETIME` | `long` | `1800000` (30 minutes) | Maximum lifetime of a connection in the pool (in milliseconds) |
| `DEFAULT_INITIALIZATION_FAIL_TIMEOUT` | `long` | `30000` (30 seconds) | Timeout for pool initialization failure (in milliseconds) |

### General Constants

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `PROFILE_SEPARATOR` | `String` | `"-"` | Separator used in profile names |
| `UNKNOWN_DATABASE_TYPE` | `String` | `"Unknown"` | Fallback value for unknown database types |
| `NOT_AVAILABLE` | `String` | `"N/A"` | Fallback value for unavailable information |
| `SPRING_APPLICATION_NAME_PROPERTY` | `String` | `"spring.application.name"` | Spring property key for application name |
| `VALUE_TRUE` | `String` | `"true"` | String representation of boolean true |
| `VALUE_FALSE` | `String` | `"false"` | String representation of boolean false |

### Logging Message Templates

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `LOG_INITIALIZING_DATASOURCE` | `String` | `"Initializing HikariCP DataSource for {}"` | Log message when initializing a datasource |
| `LOG_UNSUPPORTED_JDBC_URL` | `String` | `"Unsupported JDBC URL: {}. Supported prefixes are: {}, {}, {}"` | Log message for unsupported JDBC URLs |
| `LOG_H2_OPTIMIZATIONS` | `String` | `"Applied H2-specific optimizations"` | Log message when applying H2 optimizations |
| `LOG_METRICS_ENABLED` | `String` | `"Metrics enabled with {}"` | Log message when metrics are enabled |
| `LOG_HIKARI_CONFIGURED` | `String` | `"HikariCP configured:"` | Log message header for HikariCP configuration |
| `LOG_POOL_NAME` | `String` | `"   Pool Name: {}"` | Log message for pool name |
| `LOG_POOL_SIZE` | `String` | `"   Pool Size: min={}, max={}"` | Log message for pool size |
| `LOG_TIMEOUTS` | `String` | `"   Timeouts: connection={}ms, idle={}ms, maxLifetime={}ms"` | Log message for timeout values |
| `LOG_LEAK_DETECTION` | `String` | `"   Leak Detection: {}"` | Log message for leak detection status |
| `LOG_DATABASE_URL` | `String` | `"   Database URL: {}"` | Log message for database URL |
| `LOG_DRIVER` | `String` | `"   Driver: {}"` | Log message for database driver |
| `LOG_APPLICATION_NAME` | `String` | `"   Application Name: {}"` | Log message for application name |

### PostgreSQL-Specific Configuration

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `PG_TCP_KEEP_ALIVE` | `String` | `"tcpKeepAlive"` | PostgreSQL TCP keep-alive property |
| `PG_APPLICATION_NAME` | `String` | `"ApplicationName"` | PostgreSQL application name property |
| `PG_ASSUME_MIN_SERVER_VERSION` | `String` | `"assumeMinServerVersion"` | PostgreSQL minimum server version property |
| `PG_ASSUME_MIN_SERVER_VERSION_VALUE` | `String` | `"12.0"` | Minimum PostgreSQL server version |
| `PG_REWRITE_BATCHED_INSERTS` | `String` | `"reWriteBatchedInserts"` | PostgreSQL batched inserts rewrite property |
| `PG_PREPARE_THRESHOLD` | `String` | `"prepareThreshold"` | PostgreSQL prepare threshold property |
| `PG_PREPARE_THRESHOLD_VALUE` | `String` | `"5"` | Default prepare threshold value |
| `PG_PREPARED_STATEMENT_CACHE_QUERIES` | `String` | `"preparedStatementCacheQueries"` | PostgreSQL prepared statement cache queries property |
| `PG_PREPARED_STATEMENT_CACHE_QUERIES_VALUE` | `String` | `"250"` | Default prepared statement cache queries value |
| `PG_PREPARED_STATEMENT_CACHE_SIZE_MIB` | `String` | `"preparedStatementCacheSizeMiB"` | PostgreSQL prepared statement cache size property |
| `PG_PREPARED_STATEMENT_CACHE_SIZE_MIB_VALUE` | `String` | `"5"` | Default prepared statement cache size in MiB |
| `PG_MAINTAIN_TIME_STATS` | `String` | `"maintainTimeStats"` | PostgreSQL time statistics maintenance property |

### H2-Specific Configuration

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `H2_DB_CLOSE_DELAY` | `String` | `"DB_CLOSE_DELAY"` | H2 database close delay property |
| `H2_DB_CLOSE_DELAY_VALUE` | `String` | `"-1"` | Keep database open indefinitely |
| `H2_DB_CLOSE_ON_EXIT` | `String` | `"DB_CLOSE_ON_EXIT"` | H2 database close on exit property |
| `H2_DB_CLOSE_ON_EXIT_VALUE` | `String` | `"FALSE"` | Do not close database on JVM exit |
| `H2_CACHE_SIZE` | `String` | `"CACHE_SIZE"` | H2 cache size property |
| `H2_CACHE_SIZE_VALUE` | `String` | `"65536"` | Default cache size (64 KB) |
| `H2_LOCK_TIMEOUT` | `String` | `"LOCK_TIMEOUT"` | H2 lock timeout property |
| `H2_LOCK_TIMEOUT_VALUE` | `String` | `"10000"` | Default lock timeout (10 seconds) |

### URL Masking Patterns

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `URL_CREDENTIALS_PATTERN` | `String` | `"://[^@/]*@"` | Regex pattern to match credentials in JDBC URLs |
| `URL_CREDENTIALS_MASK` | `String` | `"://***:***@"` | Masked replacement for credentials |
| `URL_PASSWORD_PATTERN` | `String` | `"([?&]password=)[^&]*"` | Regex pattern to match password parameter |
| `URL_PASSWORD_MASK` | `String` | `"$1***"` | Masked replacement for password parameter |

### Other Constants

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `UNIT_MS` | `String` | `"ms"` | Unit string for milliseconds |
| `LEAK_DETECTION_DISABLED` | `String` | `"disabled"` | String indicating leak detection is disabled |

## Constructor

The class has a private constructor that throws an `AssertionError` to prevent instantiation.

```java
private DatabaseConstant() {
    throw new AssertionError();
}
```